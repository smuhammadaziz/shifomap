# API URL for the mobile app. Required when running on a physical device.
# On Android emulator we default to http://10.0.2.2:8080; on iOS Simulator to http://localhost:8080.
# Replace with your computer's LAN IP and the port your api-main runs on (e.g. 8080).
EXPO_PUBLIC_API_URL=http://192.168.1.1:8080
